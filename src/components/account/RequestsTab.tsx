import { ReactElement } from "react";

export function RequestsTab(): ReactElement {
    return (
        <section id="requestsTab">
            <h1>Requests</h1>
            
        </section>
    );
}